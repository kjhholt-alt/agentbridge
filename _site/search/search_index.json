{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"AgentBridge","text":"<p>Portable AI playtesting infrastructure for game engines.</p> <p>A spec, two reference adapters (Godot, Unity), a Python harness, and an MCP wrapper. The same agent script tests a Godot survival horror game and a Unity roguelike. Drop the adapter in. Implement one subclass. Done.</p>"},{"location":"#why-this-exists","title":"Why this exists","text":"<p>Modern game engines do not ship an \"AI agent plays my game\" tool. Every project that wants AI playtesting reinvents the same TCP+state+ input loop. AgentBridge is that loop, formalized once.</p>"},{"location":"#status-2026-05-05","title":"Status (2026-05-05)","text":"Layer Status Spec v1.0.0 shipped (<code>spec-v1.0.0</code> tag) Godot adapter shipped (16/16 conformance) Conformance suite shipped (Python, engine-agnostic) Python harness shipped (34/34 pytest) Observability shipped (4/4 pytest) MCP wrapper shipped (6/6 pytest) Unity adapter code shipped; Kruz-side compile + conformance Docs site this is it QuietWoods migration Phase 9"},{"location":"#five-minute-quickstart","title":"Five-minute quickstart","text":"<pre><code># 1. Clone\ngit clone https://github.com/kjhholt-alt/agentbridge\ncd agentbridge\n\n# 2. Install harness\npip install -e harness/python\n\n# 3. Drop the addon into a Godot 4.6 project\ncp -r adapters/godot/addons/agentbridge \\\n      /path/to/your/godot/project/addons/\n\n# 4. Add an AgentBridge node to a scene + a player in the \"player\" group\n\n# 5. Launch with the bridge\nAGENTBRIDGE=1 godot --path /path/to/your/godot/project --headless res://main.tscn\n\n# 6. Drive it from Python\npython -c \"\nfrom agentbridge import Client\nfrom pathlib import Path\ntoken = Path('&lt;userdata&gt;/agentbridge.token').read_text().strip()\nwith Client.connect('127.0.0.1', 7777, token=token) as c:\n    c.hello(agent_name='quickstart', capabilities=['step','snapshot_hash'])\n    print(c.state())\n    c.action('move_forward', value=True)\n    c.action('move_forward', value=False)\n    print(c.events())\n    c.quit()\n\"\n</code></pre> <p>That's the entire integration: 6 steps, ~5 minutes for someone who already has a Godot project running.</p>"},{"location":"#where-to-next","title":"Where to next","text":"<ul> <li>Wire protocol -- read this first to   understand the contract any adapter implements.</li> <li>Godot quickstart -- the path   most users take.</li> <li>Replay verification -- the \"this run   reproduces\" guarantee that most bug-hunting needs.</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>The canonical changelog lives at <code>CHANGELOG.md</code>.</p>"},{"location":"changelog/#010-2026-05-05","title":"0.1.0 -- 2026-05-05","text":"<p>All 9 phases shipped:</p> <ol> <li>Spec v1.0.0 + JSON schemas + protocol self-check (31 positive +    16 negative fixtures).</li> <li>Godot adapter as a Godot 4.6 addon.</li> <li>Conformance suite (engine-agnostic) -- 16/16 PASS on Godot.</li> <li>Python harness (sync + async client + replay + orchestrator + CLI)    -- 34/34 pytest.</li> <li>Observability log shipper -- 4/4 pytest.</li> <li>MCP server -- 6/6 pytest.</li> <li>Unity adapter (UPM package) -- code complete.</li> <li>Docs site (this site).</li> <li>QuietWoods migration to consume the addon.</li> </ol>"},{"location":"adapters/godot/","title":"Godot adapter","text":"<p>Source: <code>adapters/godot/addons/agentbridge/</code>.</p> <p>A self-contained Godot 4.6 addon. Drop it into your project's <code>addons/</code> directory, enable the plugin, add an <code>AgentBridge</code> node to a scene, and launch with <code>AGENTBRIDGE=1</code> set.</p>"},{"location":"adapters/godot/#install","title":"Install","text":"<ol> <li>Copy <code>adapters/godot/addons/agentbridge/</code> into <code>&lt;your-project&gt;/addons/</code>.</li> <li>Open Project Settings -&gt; Plugins, enable <code>AgentBridge</code>.</li> <li>Add a <code>Node</code> to your scene, attach    <code>res://addons/agentbridge/agent_bridge.gd</code>. Name it <code>AgentBridge</code>.</li> <li>Add a sibling <code>Node</code> with <code>agent_state_dump.gd</code> (default state    dump returns <code>player</code> + <code>time</code>).</li> <li>Add a sibling <code>Node</code> with <code>agent_input_driver.gd</code> and call    <code>register_default_actions()</code> from <code>_ready</code>.</li> </ol>"},{"location":"adapters/godot/#subclass-for-game-specific-state","title":"Subclass for game-specific state","text":"<p>Most games will subclass <code>AgentStateDump</code>:</p> <pre><code>extends AgentStateDump\nclass_name MyGameStateDump\n\nfunc extra_state() -&gt; Dictionary:\n    return {\n        \"inventory\": MyInventory.snapshot(),\n        \"objective\": MyQuests.current(),\n        \"director\": {\"threat\": Director.threat_level()},\n    }\n</code></pre> <p>Then point the AgentBridge node's state_dump at your subclass instead of the default.</p>"},{"location":"adapters/godot/#verify","title":"Verify","text":"<p>After install:</p> <pre><code>AGENTBRIDGE=1 godot --path . --headless res://main.tscn\n# In another shell:\npython ../../../spec/conformance/conformance_suite.py \\\n  --no-launch --port 7777 \\\n  --token-file &lt;userdata&gt;/agentbridge.token \\\n  --adapter-name godot --adapter-version 0.1.0\n</code></pre> <p>Expect 16/16 PASS.</p>"},{"location":"adapters/unity/","title":"Unity adapter","text":"<p>Source: <code>adapters/unity/AgentBridge/</code>.</p> <p>UPM package targeting Unity 2022.3+. Mirrors the Godot adapter behavior under the same wire protocol.</p>"},{"location":"adapters/unity/#install","title":"Install","text":"<p>Add to your project's <code>Packages/manifest.json</code>:</p> <pre><code>{\n  \"dependencies\": {\n    \"com.kruz.agentbridge\": \"https://github.com/kjhholt-alt/agentbridge.git?path=adapters/unity/AgentBridge\"\n  }\n}\n</code></pre>"},{"location":"adapters/unity/#setup","title":"Setup","text":"<ol> <li>Add a scene-persistent GameObject (e.g. <code>AgentBridgeRoot</code>).</li> <li>Attach the three components (in this order):</li> <li><code>AgentBridge.AgentBridge</code></li> <li><code>AgentBridge.AgentStateDump</code></li> <li><code>AgentBridge.AgentInputDriver</code></li> <li>Tag your player GameObject as <code>Player</code> so the default state dump    can find it.</li> </ol>"},{"location":"adapters/unity/#subclass-for-game-specific-state","title":"Subclass for game-specific state","text":"<pre><code>public class MyGameState : AgentBridge.AgentStateDump\n{\n    public override Dictionary&lt;string, object&gt; ExtraState()\n    {\n        return new Dictionary&lt;string, object&gt;\n        {\n            [\"inventory\"] = MyInventory.Snapshot(),\n            [\"weapon\"] = MyWeapons.Current(),\n        };\n    }\n}\n</code></pre> <p>Assign the subclass to <code>AgentBridge.StateDump</code> in the inspector.</p>"},{"location":"adapters/unity/#driving-inputsystem","title":"Driving InputSystem","text":"<p><code>AgentInputDriver.DriveSticky</code> and <code>DriveOneShot</code> are virtual hooks. Override them to integrate with the new InputSystem:</p> <pre><code>public class MyInputDriver : AgentBridge.AgentInputDriver\n{\n    protected override void DriveSticky(string inputAction, bool on)\n    {\n        var action = MyInputActions.asset[inputAction];\n        if (on) action.Enable(); else action.Disable();\n    }\n}\n</code></pre>"},{"location":"examples/godot-quickstart/","title":"Godot quickstart","text":"<p>5-minute path from clone to first agent-driven action.</p>"},{"location":"examples/godot-quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Godot 4.6.2-stable</li> <li>Python 3.11+</li> </ul>"},{"location":"examples/godot-quickstart/#1-clone-the-repo","title":"1. Clone the repo","text":"<pre><code>git clone https://github.com/kjhholt-alt/agentbridge\ncd agentbridge\n</code></pre>"},{"location":"examples/godot-quickstart/#2-install-the-harness","title":"2. Install the harness","text":"<pre><code>pip install -e harness/python\n</code></pre>"},{"location":"examples/godot-quickstart/#3-open-the-minimal-example","title":"3. Open the minimal example","text":"<pre><code>godot --path adapters/godot/examples/minimal_godot_project\n</code></pre> <p>Verify the addon shows up as enabled in Project Settings -&gt; Plugins.</p>"},{"location":"examples/godot-quickstart/#4-boot-with-the-bridge","title":"4. Boot with the bridge","text":"<pre><code>AGENTBRIDGE=1 AGENTBRIDGE_PORT=7777 \\\n  godot --path adapters/godot/examples/minimal_godot_project \\\n  --headless res://main.tscn\n</code></pre> <p>You should see:</p> <pre><code>[agentbridge] v1.0.0 listening on tcp://127.0.0.1:7777\n</code></pre>"},{"location":"examples/godot-quickstart/#5-drive-it","title":"5. Drive it","text":"<p>In another terminal:</p> <pre><code>python -c \"\nfrom pathlib import Path\nfrom agentbridge import Client\n\n# On Windows: %APPDATA%/Godot/app_userdata/&lt;project&gt;/agentbridge.token\ntoken = Path.home().joinpath('AppData/Roaming/Godot/app_userdata/AgentBridge Minimal/agentbridge.token').read_text().strip()\n\nwith Client.connect('127.0.0.1', 7777, token=token) as c:\n    h = c.hello(agent_name='quickstart', capabilities=['snapshot_hash'])\n    print('engine:', h.engine, h.engine_version)\n    print('session:', h.session_id)\n    print('state keys:', list(c.state().keys()))\n    print('hash:', c.snapshot_hash())\n    c.quit()\n\"\n</code></pre> <p>Expected output:</p> <pre><code>engine: godot 4.6.2-stable (official)\nsession: sess-&lt;n&gt;-&lt;m&gt;\nstate keys: ['player', 'ticks', 'time']\nhash: &lt;16 hex chars&gt;\n</code></pre> <p>You're now driving Godot from Python. Anything more is just more verbs and more state.</p>"},{"location":"examples/godot-quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Subclass <code>AgentStateDump</code> to expose your game's state.</li> <li>Run the conformance suite to verify your adapter integration.</li> <li>Read Replay verification to add   determinism testing to CI.</li> </ul>"},{"location":"examples/unity-quickstart/","title":"Unity quickstart","text":"<p>5-minute path from package install to first agent-driven action.</p>"},{"location":"examples/unity-quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Unity 2022.3+</li> <li>Python 3.11+</li> </ul>"},{"location":"examples/unity-quickstart/#1-install-the-package","title":"1. Install the package","text":"<p>In your Unity project's <code>Packages/manifest.json</code>:</p> <pre><code>{\n  \"dependencies\": {\n    \"com.kruz.agentbridge\": \"https://github.com/kjhholt-alt/agentbridge.git?path=adapters/unity/AgentBridge\"\n  }\n}\n</code></pre> <p>Reload Unity. The package shows in Window -&gt; Package Manager.</p>"},{"location":"examples/unity-quickstart/#2-add-the-bridge-components","title":"2. Add the bridge components","text":"<ol> <li>Create a scene-persistent GameObject named <code>AgentBridgeRoot</code>.</li> <li>Add the three components:</li> <li><code>AgentBridge.AgentBridge</code></li> <li><code>AgentBridge.AgentStateDump</code></li> <li><code>AgentBridge.AgentInputDriver</code></li> <li>Tag your player as <code>Player</code>.</li> </ol>"},{"location":"examples/unity-quickstart/#3-build-a-headless-variant-optional-but-recommended","title":"3. Build a headless variant (optional but recommended)","text":"<p>Unity Editor mode also works for testing. For CI/automation, build a Linux/Windows headless server build.</p>"},{"location":"examples/unity-quickstart/#4-launch-with-the-bridge","title":"4. Launch with the bridge","text":"<pre><code>AGENTBRIDGE=1 AGENTBRIDGE_PORT=7777 \\\n  YourGame.exe -batchmode -nographics\n</code></pre> <p>The Unity console shows:</p> <pre><code>[agentbridge] v1.0.0 listening on tcp://127.0.0.1:7777\n</code></pre>"},{"location":"examples/unity-quickstart/#5-drive-it","title":"5. Drive it","text":"<pre><code>from pathlib import Path\nfrom agentbridge import Client\n\ntoken = Path.home().joinpath('AppData/LocalLow/&lt;Company&gt;/&lt;Game&gt;/agentbridge.token').read_text().strip()\n\nwith Client.connect('127.0.0.1', 7777, token=token) as c:\n    h = c.hello(agent_name='quickstart')\n    print(h.engine, h.engine_version)\n    print(c.state())\n    c.quit()\n</code></pre> <p>Expected:</p> <pre><code>unity 2022.3.x\n{'player': {'position': [...]}, 'time': {...}, 'ticks': N}\n</code></pre>"},{"location":"harness/cli/","title":"CLI","text":"<p>After <code>pip install -e harness/python</code>, <code>agentbridge</code> is on PATH.</p>"},{"location":"harness/cli/#subcommands","title":"Subcommands","text":"Command Purpose <code>agentbridge run</code> run a single mission against a launched or running adapter <code>agentbridge replay &lt;ndjson&gt;</code> verify recorded session is deterministic <code>agentbridge self-check</code> validate spec schemas + run fixtures"},{"location":"harness/cli/#examples","title":"Examples","text":"<pre><code># self-check (no adapter required)\nagentbridge self-check\n\n# run a scripted mission\nagentbridge run \\\n  --port 7777 \\\n  --token-file &lt;userdata&gt;/agentbridge.token \\\n  --script my_script.json\n\n# replay verification\nagentbridge replay session_123.ndjson \\\n  --port 7777 \\\n  --token-file &lt;userdata&gt;/agentbridge.token\n</code></pre>"},{"location":"harness/python/","title":"Python harness","text":"<p>Install:</p> <pre><code>pip install -e harness/python\n</code></pre>"},{"location":"harness/python/#sync-client","title":"Sync client","text":"<pre><code>from agentbridge import Client\nfrom pathlib import Path\n\ntoken = Path(\"&lt;userdata&gt;/agentbridge.token\").read_text().strip()\nwith Client.connect(\"127.0.0.1\", 7777, token=token) as c:\n    h = c.hello(agent_name=\"my-script\",\n                capabilities=[\"set_seed\", \"snapshot_hash\"])\n    print(h.engine, h.engine_version, h.session_id)\n    state = c.state()\n    c.action(\"move_forward\", value=True)\n    c.action(\"move_forward\", value=False)\n    events = c.events()\n    c.quit()\n</code></pre>"},{"location":"harness/python/#async-client","title":"Async client","text":"<pre><code>import asyncio\nfrom agentbridge import AsyncClient\n\nasync def main():\n    c = await AsyncClient.connect(\"127.0.0.1\", 7777, token=\"...\")\n    await c.hello(\"agent\")\n    print(await c.state())\n    await c.quit()\n    await c.close()\n\nasyncio.run(main())\n</code></pre>"},{"location":"harness/python/#replay-verifier","title":"Replay verifier","text":"<pre><code>from pathlib import Path\nfrom agentbridge import verify\n\nreport = verify(\"127.0.0.1\", 7777, token=\"...\",\n                recorded_path=Path(\"session.ndjson\"))\nassert report.deterministic, f\"diverged at {report.first_divergence_at}\"\n</code></pre> <p>The verifier replays the recorded inputs against a fresh adapter, comparing <code>snapshot_hash</code> sequences. Divergence indicates a real non-determinism bug.</p>"},{"location":"harness/python/#coverage","title":"Coverage","text":"<p>Phase 4 ships the client + replay + orchestrator + CLI. Test coverage is on the public API; conformance is the integration test.</p>"},{"location":"harness/replay/","title":"Replay verification","text":"<p>The most useful guarantee AgentBridge provides: the same inputs in the same world produce the same outputs. Every regression hunt benefits from this.</p>"},{"location":"harness/replay/#how-it-works","title":"How it works","text":"<ol> <li>Start a session against the adapter with the <code>replay</code> capability    negotiated. The adapter writes every command + response + event    to <code>&lt;userdata&gt;/agentbridge/&lt;session_id&gt;.ndjson</code>.</li> <li>Run any agent (mission / script / human) to drive a meaningful    sequence. Periodically call <code>snapshot_hash</code> -- those hashes are    the ground truth.</li> <li>After the session, copy the ndjson somewhere stable.</li> <li>Run <code>agentbridge replay &lt;ndjson&gt;</code> against a fresh adapter (with    <code>set_seed</code> + <code>snapshot_hash</code> capabilities). The harness replays    the inputs and asserts the snapshot hashes match.</li> </ol>"},{"location":"harness/replay/#determinism-is-opt-in","title":"Determinism is opt-in","text":"<p>The base <code>AgentStateDump</code> includes timestamps in <code>time</code>, which makes hashes drift across runs by design. For deterministic replay:</p> <ul> <li>Game uses <code>set_seed</code> to reset all PRNGs.</li> <li>State dump excludes wall-clock fields (override <code>_time_dump</code> in   Godot, <code>TimeDump</code> in Unity).</li> <li>All non-seeded inputs (camera shake amplitudes, particle emit   rates) are seeded.</li> </ul> <p>Then <code>snapshot_hash</code> is byte-stable for the same inputs.</p>"},{"location":"harness/replay/#catching-regression","title":"Catching regression","text":"<p>Drop the previous-known-good ndjson in your CI tree. Every PR runs <code>agentbridge replay &lt;recorded&gt;.ndjson</code>. If a code change breaks determinism, the divergence index points to the exact step.</p>"},{"location":"spec/conformance/","title":"Conformance suite","text":"<p>A black-box test runner at <code>spec/conformance/conformance_suite.py</code>.</p>"},{"location":"spec/conformance/#what-it-tests","title":"What it tests","text":"<p>16 scenarios (as of v0.1.0):</p> <ul> <li><code>handshake_required</code> -- pre-hello commands return code 1001</li> <li><code>auth_failure</code> -- bad token returns code 1002</li> <li><code>hello_negotiates_caps</code> -- session_id + server_caps returned</li> <li><code>ping_after_hello</code></li> <li><code>state_has_base_keys</code> -- <code>player</code>, <code>time</code> always present</li> <li><code>action_unknown_returns_2001</code></li> <li><code>sticky_press_release</code>, <code>oneshot_pulse</code>, <code>look_delta_returns_ok</code></li> <li><code>events_drain</code>, <code>subscribe_unsubscribe</code></li> <li><code>set_seed_when_negotiated</code>, <code>snapshot_hash_format</code></li> <li><code>metrics_returned</code>, <code>capabilities_echo</code></li> <li><code>quit_clean_disconnect</code></li> </ul>"},{"location":"spec/conformance/#run-against-any-adapter","title":"Run against any adapter","text":"<pre><code># Adapter must already be running on host:port\npython spec/conformance/conformance_suite.py \\\n  --no-launch \\\n  --host 127.0.0.1 --port 7777 \\\n  --token-file &lt;userdata&gt;/agentbridge.token \\\n  --adapter-name &lt;engine&gt; --adapter-version &lt;v&gt; \\\n  --report conformance-&lt;engine&gt;-&lt;v&gt;.md\n</code></pre>"},{"location":"spec/conformance/#reference-results","title":"Reference results","text":"Adapter Version Score Report Godot 0.1.0 16/16 <code>adapters/godot/conformance-godot-0.1.0.md</code> Unity 0.1.0 pending Kruz-side compile --"},{"location":"spec/protocol/","title":"Wire protocol v1.0.0","text":"<p>Source of truth: <code>spec/AGENTBRIDGE_SPEC.md</code>.</p> <p>This page is a friendly tour of the canonical spec. Read this first; read the spec when you need to implement an adapter or write a non-Python client.</p>"},{"location":"spec/protocol/#three-primitives","title":"Three primitives","text":"<ol> <li>Handshake + auth. Client sends <code>hello</code> with a per-launch token.</li> <li>State + actions. Client polls <code>state</code>, sends <code>action</code> commands.</li> <li>Events. Client drains <code>events</code> (or subscribes via    <code>subscribe</code>).</li> </ol> <p>Add deterministic primitives via capability negotiation: <code>set_seed</code>, <code>step</code>, <code>snapshot_hash</code>, <code>timescale</code>. These let you replay sessions byte-for-byte.</p>"},{"location":"spec/protocol/#why-tcp-ndjson","title":"Why TCP + NDJSON?","text":"<ul> <li>Universally supported in every game engine + every language.</li> <li>Newline-delimited JSON is human-readable in tcpdump.</li> <li>No schemas to compile -- every line is self-describing.</li> <li>Async-friendly (line oriented).</li> </ul> <p>WebSocket and stdio are alternative transports declared via capabilities <code>transport.websocket</code> and <code>transport.stdio</code>.</p>"},{"location":"spec/schemas/","title":"JSON schemas","text":"<p>All four v1 schemas live at <code>spec/schema/v1/</code>.</p> Schema Purpose <code>commands.json</code> Every JSON line sent client -&gt; adapter <code>responses.json</code> Every JSON line sent adapter -&gt; client <code>events.json</code> Every event entry returned by <code>events</code> <code>state.json</code> Base shape every adapter MUST emit on <code>state</code>"},{"location":"spec/schemas/#validate-locally","title":"Validate locally","text":"<pre><code>pip install jsonschema\npython -m agentbridge.protocol --self-check\n</code></pre> <p>Self-check loads all 4 schemas, runs 31 positive + 16 negative fixtures, exits 0 if everything matches the spec.</p>"},{"location":"spec/schemas/#backwards-compat","title":"Backwards compat","text":"<p>After v1.0.0, schema changes are additive only. Major version bumps require a new <code>schema/v2/</code> directory while keeping v1 served for at least 12 months. Adapters MAY support multiple protocol versions concurrently.</p>"}]}